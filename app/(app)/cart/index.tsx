import { CartScreen } from "@/components/cart/CartScreen";

/** Pushed route (from checkout, product detail, negotiations) — keeps a back button. */
export default function PushedCartScreen() {
  return <CartScreen />;
}
